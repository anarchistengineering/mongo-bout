const {
  Bout,
  matches,
  filter,
} = require('./lib/bout');

module.exports = {
  curry: require('./lib/curry'),
  utils: require('./lib/utils'),
  Parser: require('./lib/parser'),
  Bout,
  matches,
  filter,
};
